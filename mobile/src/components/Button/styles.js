import styled from 'styled-components/native';
import { RectButton } from 'react-native-gesture-handler';

export const AButton = styled(RectButton)`
  align-items: center;
  justify-content: center;
  height: 45px;
  border-radius: 4px;
  background: #ee4e62;
`;

export const BText = styled.Text`
  font-size: 16px;
  font-weight: bold;
  color: #fff;
`;
